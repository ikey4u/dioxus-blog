# Setup

In one terminal

    cd dioxus-blog/fe
    npx tailwindcss -i src/main.css -o public/tailwind.css --watch

In another terminal

    cd dioxus-blog/fe
    dx serve --hot-reload --platform web

